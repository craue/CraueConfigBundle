imports:
  - { resource: config.yml }

framework:
  cache:
    pools:
      craue_config_cache:
        adapter: my_filesystem_cache_adapter

services:
  my_filesystem_cache_adapter:
    parent: cache.adapter.filesystem
    tags:
      - { name: cache.pool, namespace: craue_config }
